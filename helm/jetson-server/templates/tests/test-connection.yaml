apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jetson-server.fullname" . }}-test-connection"
  labels:
    {{- include "jetson-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jetson-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
